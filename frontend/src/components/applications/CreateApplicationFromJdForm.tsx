"use client";

export function CreateApplicationFromJdForm({ onCreated }: { onCreated: () => void }) {
  return (
    <div className="text-sm text-muted-foreground">
      Paste a job description to generate a draft — coming next.
    </div>
  );
}
