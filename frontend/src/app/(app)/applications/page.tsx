// ApplicationsPage: placeholder for the main dashboard (will connect to GET /applications later).

export default function ApplicationsPage() {
  return <main className="text-sm">TODO: Applications dashboard</main>;
}
