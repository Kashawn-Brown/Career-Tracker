# Local dev (Docker Compose Postgres uses host port 5437)
DATABASE_URL="postgresql://postgres:postgres@localhost:5437/career_tracker?schema=public"

# Auth
JWT_SECRET="dev_change_me"

# Google OAuth
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
# Must match Google Console "Authorized redirect URIs"
GOOGLE_OAUTH_REDIRECT_URI="http://localhost:3002/api/v1/auth/oauth/google/callback"


# Redis (recommended for Cloud Run-safe rate limiting)
# Upstash example: rediss://:<PASSWORD>@<HOST>:<PORT>
REDIS_URL=


# CORS (comma-separated)
# Dev: include localhost
# Prod (Cloud Run): use ONLY the deployed frontend origin
CORS_ORIGIN="http://localhost:3000,https://career-tracker-frontend-ten.vercel.app"

# Flags
ENABLE_DEBUG_ROUTES="true"
NODE_ENV="development"

# Documents v1 (GCS uploads)
GCS_BUCKET="documents-career-tracker-482222"

# Use | not comma in prod (gcloud --set-env-vars uses commas to separate vars)
GCS_ALLOWED_MIME_TYPES="application/pdf|text/plain"

# 10 MB
GCS_MAX_UPLOAD_BYTES="10485760"

# 10 minutes
GCS_SIGNED_URL_TTL_SECONDS="600"

# Key prefix for GCS
GCS_KEY_PREFIX="dev"

# 
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Email (Phase B)
RESEND_API_KEY=
EMAIL_FROM=
FRONTEND_URL="http://localhost:3000"

# Where Pro request notification emails are sent (owner inbox)
OWNER_EMAIL=owner@email.com



# Cloud Run notes:
# - Cloud Run provides PORT automatically (typically 8080)
# - For Cloud SQL via unix socket, DATABASE_URL will look like:
#   postgresql://USER:PASSWORD@localhost:5432/career_tracker?schema=public&host=/cloudsql/PROJECT:REGION:INSTANCE
